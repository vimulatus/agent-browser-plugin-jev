{
  "name": "agent-browser-plugin-jev",
  "version": "0.0.0",
  "description": "agent-browser plugin: Jev drives the browser from one goal or one policy",
  "type": "module",
  "license": "Apache-2.0",
  "repository": "github:vimulatus/agent-browser-plugin-jev",
  "packageManager": "pnpm@11.1.3",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "agent-browser-plugin-jev": "./dist/main.js"
  },
  "files": [
    "dist",
    "policies"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm run build && node --test test/*.test.mjs"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "typescript": "^7.0.2"
  }
}
